# features

Frontend code grouped by feature.

Each feature may include `components`, `api`, `hooks`, `store`, `types`, `utils`, and `mock-data` as needed. Code that would become hard to maintain when mixed with other features belongs here.
