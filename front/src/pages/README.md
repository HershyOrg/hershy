# pages

Folder for route-level screen components.

`StrategyBuilderPage.tsx` currently separates the screen logic that used to live in `App.tsx` into a page. When a router is added, routes should connect at the page level.
