# shared

Cross-feature code goes here.

Use this folder for code that can be reused by more than one feature, such as common UI components, generic hooks, formatting helpers, API client wrappers, and global types.

Do not put strategy-exchange-specific business logic here. Keep that inside `features/strategy-exchange/`.
