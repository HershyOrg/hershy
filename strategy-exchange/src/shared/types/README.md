# shared/types

Global TypeScript types used by multiple features.

Examples:
- common API response envelopes
- shared ID/address primitives
- generic pagination types
- app-wide user/session types

Strategy-specific types such as `Strategy`, `Creator`, `Sector`, or `AdapterViewMode` belong in `features/strategy-exchange/types/`.
