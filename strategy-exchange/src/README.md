# src

Vite React app source root.

Put only app entry files and top-level folders here. Feature code should live under `features/`, route-level screens under `pages/`, and reusable cross-feature code under `shared/`.

Current entry files:
- `main.tsx`: mounts React into the DOM.
- `App.tsx`: thin app wrapper that renders the current page tree.
