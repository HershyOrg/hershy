# components

React components used only by the strategy exchange feature.

Examples:
- strategy cards
- market spotlight panels
- adapter detail panels
- creator profile blocks
- Hershy canvas preview wrappers
- strategy exchange empty/not-found views

Do not put generic buttons, modals, or reusable app chrome here. Put those in `shared/components/`.
