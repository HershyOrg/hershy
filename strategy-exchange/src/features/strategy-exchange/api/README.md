# api

Strategy exchange API request layer.

Put endpoint builders, request functions, and response types for strategy exchange data here.

Examples:
- strategy feed request
- vault metadata request
- user account request
- user profile update request
- vault discussion request

Generic fetch helpers belong in `shared/api/`.
