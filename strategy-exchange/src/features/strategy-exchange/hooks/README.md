# hooks

React hooks for strategy exchange state and data loading.

Examples:
- loading strategy feed data
- loading vault metadata
- loading vault discussions
- loading user account data

Hooks in this folder can call `features/strategy-exchange/api/` and return UI-ready loading/data/error state.
