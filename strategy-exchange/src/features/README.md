# features

Feature modules live here.

Each feature should contain the code that belongs to one product capability: feature-specific components, API calls, hooks, local store, types, utilities, and animations.

Add a new folder for each large product area, for example:
- `strategy-exchange/`
- `strategy-editor/`
- `portfolio/`
